[project]
name = "gtfs_binary_scripts"
version = "0.1.0"
description = "A collection of scripts to manage a GTFS Binary catalogue"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gtfs-binary",
    "pyyaml>=6.0.3",
    "shapely>=2.1.2",
]

[tool.uv.sources]
gtfs-binary = { git = "https://github.com/Zverik/gtfs-binary.git" }
